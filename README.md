A blazingly crazily fast image hosting service written in Rust.

Env example:
```
POSTGRES_URI="<uri>"
BIND_ADDRESS="127.0.0.1:9810"
DEFAULT_USERNAME="<default user's name>"
DEFAULT_PASSWORD="<default user's password>"
```